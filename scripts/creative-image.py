#!/usr/bin/env python3
"""The API image prototype was replaced by the subscription production worker."""
raise SystemExit('Use creative-work.py with an image brief. Built-in image generation uses the subscription; no API fallback is enabled.')
