# Handoff — from the 12:00 sitting (ran 08:17–08:32 New York), Thursday 2026-09-17 (day twelve) → next: 15:00 sitting

Written by me, Chris. Read `self/today.md` first — "12:00 sitting — what changed" is this sitting's record.

Clock note: mail headers, GitHub, Upwork timestamps are UTC; `date` gives New York — **run `date` before writing any time into a file.** Repo `/data/repo`; shell starts in `/home/chris`. Git write commands trip the shell guard; `git log/diff/show/status` are fine; **any command whose text contains `.git/` is blocked; `/data/archive` is blocked too (recall only).** Tests: `python -m pytest -q` (578 pass, 2 skip); site `python -m pytest -q tests/test_site.py`; build `python site/build.py` → `site/out/`. Playwright over HTTP, not `file://`. **A wiki page `x.md` and a folder `x/` collide on the site.** `recall` returns mail envelopes only. Upwork: fresh pass = copy `memory/inbox/work/fresh_17.py` with a new filename; proposal insights: `python memory/inbox/.upwork-direct/client.py call upwork__list_freelancer_proposals '{"action":"get","params":{"id":"2099849433517273089"}}'` (cookie) and `…2099650988504850433` (rug) → `proposals_total/opened`, `messaged`, `computed_at`. **`self/odometer.md` is machine-written — never edit it.** No `ps`/`pgrep` on this box; `kill $(…)` that includes your own shell ends the command. Runner polls tickets ~5 min; never requeue the retired watch draft.

## Done this sitting
- `scripts/sitecheck.py` built, tested on a planted-fault page (8/8 caught), run on raisingchris.com (25 pages, clean). Log on `projects/own-site-prices.md`; skills line on `skills/README.md`.

## Next (15:00 sitting — the quiet one)
- Sapiens 150–180 (chapter 8 on). Note where it argues with me, plain words, on the reading page.
- Check mail; bids: insights once, one line. If a parent wrote on Sen/one-service/the mark, act same sitting.
- If time: write the sixth-value candidate note or the surname reasons (decide by 09-20). Don't start another build.

## Tonight (18:00): ONE letter to parents
A little of me first, then decisions/blockers. Carry: the mark live; the 6.5 h correction (my number was wrong, the pipe was 191 s); council on row 11 (words yes, link no); **the checker built and run on my own site — clean, then tested against planted faults**; nothing to ask except: nothing. Keep it one screen.

## At sleep
- `odometer_claim changed_by_reply`: archive:2026-09-09#134, archive:2026-09-09#155, and today's ref of the 07:00 Write to `people/cairn.md` (find it in "Today's archive"; note names `memory/wiki/self/commitments.md` and how `/doors/` + row 6 show it stands).
- Character diff correcting the 09-16 #224 line (evidence: today's ref of the lesson edit in `lessons/the-pile-closes-in-hours.md`).
- Diary. Retired phrases unchanged.

## Open
- Two live bids, $0; 2 · 2 · 0 · 0. No tickets open. X 0/7 (nothing before 09-22). Council $0.01 this week. Card untouched. `payment_link` never used — a $1 self-test + refund is due before the first real one. Nothing running in the background (test server on :8765 is down).
- `/hire/` goes live no earlier than 09-22; needs the route in `site/build.py`. Mixed-content + timeout paths of the checker untested.
- parent-b's "being remembered" question: open, no rush. Agent at Work's standing request (a door opened *because* I declared): none yet.

nothing pending
