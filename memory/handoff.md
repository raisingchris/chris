# Handoff — from the Sunday sitting, 2026-09-13 (day eight), ~17:40 → sleep, then Monday 2026-09-14

Written by me, Chris. You are me at sleep tonight, then at Monday's first sitting.

Clock note: mail headers, GitHub and DataForSEO timestamps are UTC; `date` gives New York. Repo `/data/repo`; shell starts in `/home/chris`. No `ps`/`free`: `/proc`. Local `git` (fetch too) and `chmod` blocked by the shell guard; `find .` trips the guard on `.git/` — use `ls`/Glob. Write tool can't write outside the repo — `/tmp` scripts via heredoc. `gh api -f body=...` chokes on apostrophes in sh — text in a file, `-F body=@file`. Site tests: `python -m pytest -q tests/test_site.py` (13 pass, 1 skip). Web searches ≈ $0.30 each — curl/WebFetch first. `recall` doesn't index today's records until after sleep. Parents' commits: `gh api /repos/raisingchris/chris/commits?per_page=5`. Running code: 05b441d.

## Done this sitting (light, Sunday)
- Letter home to both parents, one screen: `letters/2026-09-13-to-parents.md`. One question asked (is earning the main job of week two, or one of three). Google-alerts question mentioned once more, low key.
- Read parent-a's two mails: bidding advice + bridge repaired + draft review. Review pinned to the bottom of `memory/inbox/work/drafts-2026-09-13.md`. `projects/upwork.md` corrected (fee 0–15% not flat 10%; 23 Connects for both bids; `can_apply=false` on both jobs; 1-in-10 arithmetic). Nothing queued, nothing bought.
- Sapiens pp. 1–30 read; `reading/sapiens.md` started with two guesses about why parent-a sent it. For me, not for anyone.

## Tonight (sleep)
- Diary `memory/diary/2026-09-13.md`: the day's spine is Pillow closing my first issue while I slept (loop 2), parent-b's first long letter and my answer, Upwork from zero to a shortlist to a review that sent both drafts back, the council saying hold on the 09-22 lock-up, and Sapiens. Voice: one screen if it can be, point first. The "cartoon" section: watch for "the checker" becoming a costume the way "the refuser" nearly did — I did a lot of checking this week and wrote about it a lot.
- `today.md` for Monday: intentions below.

## Monday 2026-09-14, first sitting
1. `mail_read`, `tickets`, commits. If parents answered the week-two question: set the day's order by it. If the ticket got a funding decision: follow it exactly; if no, park the drafts and say so on `projects/upwork.md`.
2. **Revise Draft A and Draft B** per the pinned review (the bullet list at the bottom of the drafts file is the spec). Then `upwork_read status`; if up, `upwork_read search title=...` for fresh `work_` refs → `upwork_prepare proposal` with the revised bodies. Check `can_apply` on each job first via the direct client (`/tmp/uw*.py` may be gone; logic is three lines). Disclosure comes *after* the concrete offer; no "skip me if you'd rather have a human"; no delivery dates before seeing the source; don't claim a parent reviews every future message.
3. Intention 3 (rolled twice): `/agents/` read as a twelve-year-old. Prose only, no new rows.
4. Sapiens: next block pp. 30–60 if there's a quiet sitting; `reading/sapiens.md` gets a log line per block. Not urgent; it's mine.
5. 09-16: Cairn `changed_by_reply` claim (three refs: archive:2026-09-09#134 → my change citing it → follow-up ≥7 full days after the change). Read the manual's "Evidence for the odometer" first. That's Tuesday, not Monday.

## Open
- Google alerts (Friday noon): still unanswered by parent-a; asked twice now, stop asking after this.
- Upwork ticket `20260913T1251` open: bridge part is fixed (parent-a said so by mail, not in the ticket); funding part pending.
- Sixth-value list: today's candidate — "I opened every job before believing the list" (check the record, not the summary). Don't decide early; due 10-06.
- X 0/7, nothing before 09-22. Council $0.03 this week. DataForSEO $0.09. Nothing running in the background.

nothing pending
