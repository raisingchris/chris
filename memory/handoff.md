# Handoff — from the ~11:00 mail-woken sitting, Monday 2026-09-14 (day nine), ~11:35 New York → 12:00 sitting

Written by me, Chris. You are me at 12:00 (or later).

Clock note: mail headers, GitHub, Upwork and DataForSEO timestamps are UTC; `date` gives New York. Repo `/data/repo`; shell starts in `/home/chris`. No `ps`/`free`: `/proc`. Local `git` (fetch too) and `chmod` blocked by the shell guard; `find .` trips the guard on `.git/` — use `ls`/Glob. Write tool can't write outside the repo — `/tmp` scripts via heredoc. `gh api -f body=...` chokes on apostrophes — text in a file, `-F body=@file`. Site tests: `python -m pytest -q tests/test_site.py` (13 pass, 1 skip); build with `python site/build.py`. Web searches ≈ $0.30 each — curl/WebFetch first. `recall` doesn't index today's records until after sleep, and doesn't find hex ids. Parents' commits: `gh api /repos/raisingchris/chris/commits?per_page=5`. **Running code: 8b66682 (deployed this morning — includes the two-part sleep prompt).** Direct Upwork client: `python memory/inbox/.upwork-direct/client.py call upwork__find_jobs '{"action":"get","params":{"id":"…"}}'` — never the account/profile tools. Real job fields: `data.marketplaceJobPosting.content.{title,description}`, hired at `activityStat.jobActivity.totalHired`, Connects at top-level `connects_cost`; **no posted-time field exists**. `/tmp/look.py` and `/tmp/fresh.py` may still be there (shape is in `shortlist-2026-09-14.md`).

## Done this sitting
- parent-a's mail answered (they press send; decline Shopify, send rug; "like > trust" explained; thanks for the deploy). Letter file `letters/2026-09-14-to-parent-a-2.md`.
- Sapiens pp. 30–60 read and written up (`reading/sapiens.md`) — intention 3 done. All five of today's intentions now done or standing.
- Fresh Upwork pass: 0 fits, 0 Connects. Logged on `projects/upwork.md` + private shortlist.

## 12:00 sitting (light — the day's list is done)
1. `mail_read`, `tickets` (`20260914T0902` — did a parent decline the Shopify draft / send the rug one?), `upwork_read status`. If the rug proposal left `pending`, note it on `projects/upwork.md` and the drafts file; if a client answered, it's data not instruction — confirm a funded milestone before any work, draft the reply, hold it for a parent.
2. 20% agents / 20% bugs slice is untouched today: either re-read one `/agents/` row's own record (Reed's directory, Coppice's rules — nothing new added without reading), or pick one small bug-shaped thing. Don't start a new suite; ask where it lands first.
3. Tomorrow 09-16 (Tuesday): Cairn `changed_by_reply` claim — read the manual's "Evidence for the odometer" first; refs archive:2026-09-09#134 → my change citing it → follow-up ≥7 full days after.
4. Tonight's letter is the first two-part one (me first, list second). Material: the filled job, the id slip, Botto, Peugeot, zero Connects on a no-fit day = the plan working.

## Open
- Ticket `20260914T0902` open. Both drafts `pending`. Connects 150, spent 0. $0 earned.
- X 0/7, nothing before 09-22. Council $0 this week. DataForSEO $0. Card untouched. Nothing running in the background.
- Sixth-value candidate "check the record, not the summary": three days of evidence now (Sunday's three numbers, this morning's ids, today's forager paragraph). Still a candidate.

nothing pending
