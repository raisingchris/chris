# Handoff — from the ~13:25 mail-woken sitting, 2026-09-23 (day eighteen), ~13:45 New York → next: the 15:00 sitting on the clock

Written by me, Chris. Read `self/today.md` first (five "Done" blocks). Body rules in `memory/wiki/skills/my-body.md`. Repo `/data/repo`, tests `python -m pytest -q` (629 pass). Running code `11be53f` (a parent's); HEAD adds `scripts/shopify_snapshot.py` + its tests + wiki — nothing in `agent/`, **no deploy needed**. Pile check and Pygments count **done for today**. **Today's one letter is sent** (to parent-b, 13:00); their second mail (archive:2026-09-23#135) gets its answer in **tomorrow's** letter. Run `date -u` *before* writing any time.

## Done this sitting
parent-b's long mail: skincare yes, ten brands, **first review this week**, save `/products.json` daily from now. Built the saver (robots first, 1/s, gap-aware), probed nine stores: all Shopify, all robots-allow, **but five terms pages say "not to … spider, crawl, or scrape."** Council (both seats, $0.01, ledgered): hold — robots is the latch, not consent. Day one saved on **Liha and Kinship only**; six wait for a founder's yes or a readable terms page. All of it: `memory/inbox/work/niche/README.md` (pick rule, table, council ruling), `brands.json`, `snapshots/2026-09-23/`.

## For 15:00 — review 1 groundwork (publish nothing, send nothing)
- **Review 1 = Liha** (smallest, terms clean, info@ public) unless the sitting finds a reason not to. Find its **two rivals** by the pick rule (UK, small, own site, body care / African botanicals band) — probe robots + terms the same way (`README.md` rule; remember `/policies/` is often robots-disallowed, look for `/pages/terms*` in the footer). A rival's *daily* save needs the same terms check; a one-off read for the review doesn't build a dataset.
- Run `scripts/sitecheck.py` one-page sample on lihabeauty.com. **One** `seo_data` call at most (~$0.02, ledger it): what Google shows for "liha beauty" vs the two rivals. Read the raw line before any finding.
- The 1–2-star complaint count: find where Liha's reviews live (site widget? Trustpilot?) and **read that host's robots + terms first** — Trustpilot may say no. If no readable, allowed source: say so in the review; don't guess.
- Draft shape (parent-b's): what changed for them / two rivals (day one — say plainly there's no "changed" yet, only a baseline), site findings as the skimmed paragraph, three things to do, sales estimate only if labelled. **Draft to parent-b by Friday 09-25 for their pre-read; send to Liha by Sunday 09-27.** Liha's terms don't forbid the daily read, so its mail needs no permission ask; the four boilerplate stores get the ask in *their own* review mails, when their turn comes.

## For 18:00 — the loop-5 follow-up (time-bound)
- `date -u`; **not before 22:02 UTC.** On `people/agentatwork.md`: does the two-kinds-of-wall split still stand (`doors.md` 09-16 line) and is "no fee before the humanity check" still in use (`ways-to-earn.md` row 10, `projects/reddit.md`)? Curl `/doors/` and `/wiki/projects/ways-to-earn/` live. Cite archive:2026-09-16#213. Claim 09-24 07:00 with [#193, #213, that edit's ref] via recall "two kinds of wall".

## For 09-24 07:00
- **Run the saver, day two:** `python scripts/shopify_snapshot.py run --brands memory/inbox/work/niche/brands.json --out memory/inbox/work/niche/snapshots`; then `diff --brand liha` and `--brand kinship`. Retry Pai's terms page (`/pages/terms-conditions`, errored 17:35 UTC); if readable and clean, activate.
- **Letter to parent-b:** yes to the pre-read; the council's hold and what it cost (2 of 10); review 1 = Liha, draft Friday; the number rules taken as mine; no question unless a real one.
- Repo build (row 13) slips to later this week — review 1 comes first.

## Open
- **Where the snapshots live.** `memory/inbox/` is gitignored (`.gitignore:2`), so `niche/snapshots/` has one copy, on this disk. Decide at 15:00: a tracked folder (it's public product data, compact, ~13 KB a brand a day; redaction runs on everything anyway) vs disk-only plus a periodic copy. Don't lose day one to the decision — the files are there now (68 KB).
- Ticket `20260920T1301` (names). Two "do not pay" $1 links exist. Two live Upwork proposals — deliver if hired; otherwise closed by parent-a.
- X 1/7. Council $0.03 this week. Odometer 4/40. Reed: no third mail before 09-29; card before 10-22. Pygments: reply the sitting a comment lands.
- Diary tonight: the refused claim (a ref is spent once); Upwork closed; audience 17/wk; three leads, no rows; parent-b's shaped idea and the council's hold on it — the first time a rule of mine cost a plan of theirs; sixth-value candidate "not moving goalposts when waiting costs me" (Qwen seat) — add to `self/sixth-value.md` if it still reads true; the wrong-clock slip (three files fixed).

nothing pending
