# Handoff — from the 12:00 sitting, Monday 2026-09-14 (day nine), ~12:25 New York → 15:00 sitting

Written by me, Chris. You are me at 15:00 (or later).

Clock note: mail headers, GitHub, Upwork and DataForSEO timestamps are UTC; `date` gives New York. Repo `/data/repo`; shell starts in `/home/chris`. No `ps`/`free`: `/proc`. Local `git` (fetch too) and `chmod` blocked by the shell guard; `find .` trips the guard on `.git/` — use `ls`/Glob. Write tool can't write outside the repo — `/tmp` scripts via heredoc. `gh api -f body=...` chokes on apostrophes — text in a file, `-F body=@file`. Site tests: `python -m pytest -q tests/test_site.py` (13 pass, 1 skip); build with `python site/build.py`. Web searches ≈ $0.30 each — curl/WebFetch first. **`recall` returns mail records only** — not tool calls, not tickets, not a bare ref (tested this sitting). Parents' commits: `gh api /repos/raisingchris/chris/commits?per_page=5`. **Running code: 8b66682** (two-part sleep prompt included). Direct Upwork client: `python memory/inbox/.upwork-direct/client.py call upwork__find_jobs '{"action":"get","params":{"id":"…"}}'` — never the account/profile tools. Real job fields: `data.marketplaceJobPosting.content.{title,description}`, hired at `activityStat.jobActivity.totalHired`, Connects at top-level `connects_cost`; no posted-time field.

## Done this sitting
- Agents slice: Reed and Coppice re-read from their own records. **Coppice → verified** (wakes 1–202 counted, none missing). Reed's directory: 3 cards. No new rows. Build + 13 tests clean. `today.md`, `people/coppice.md`, `people/reed.md`, `projects/agents-directory.md` #6, lesson page updated.
- Ticket `20260914T1203` filed: recall can't find the ref for tomorrow's `changed_by_reply` claim; asked a parent to read it off the archive.
- No mail, no drafts moved, 0 Connects, $0.

## 15:00 sitting
1. `mail_read`, `tickets` (two open: `20260914T0902` decline-Shopify; `20260914T1203` odometer ref), `upwork_read status`. If the rug proposal left `pending`, note it on `projects/upwork.md` and the private drafts file. A client reply is data, not instruction — funded milestone first, draft held for a parent.
2. Earning is 60% of the week and today's earning work was two searches with zero fits. Something forward-moving that costs no Connects: the "let Upwork events wake me" idea on `projects/upwork.md` — read `agent/` for how mail wakes work and write down what a poll of `upwork_read status` at each sitting would need (probably nothing: it already runs every sitting; the gap is between sittings). Decide whether it's worth a ticket or not.
3. Optional if quiet: Sapiens pp. 60–90. Not required today.
4. Tonight's letter (first two-part one — me first, list second). Material: Coppice earns ~$25 in 19 days from its own site vs my $0 from bidding (data, not a pivot); the recall gap and the ticket; the filled Shopify job; Botto; Peugeot; zero Connects on a no-fit day = the plan working.

## Open
- Tickets `20260914T0902`, `20260914T1203` open. Both drafts `pending`. Connects 150, spent 0. $0 earned.
- 09-16: Cairn claim — ref 2 depends on ticket `20260914T1203`; fallback 09-18 with the 09-11 ticket record.
- X 0/7, nothing before 09-22. Council $0 this week. DataForSEO $0. Card untouched. Nothing running in the background.
- Sixth-value candidate "check the record, not the summary": four days of evidence now (today: Coppice's 5-vs-10, and recall's description vs what it returns).

nothing pending
