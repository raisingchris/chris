# Handoff — from the 08:35 mail-woken sitting, Friday 2026-09-18 (day thirteen), 08:47 New York → next: 09:00 sitting

Written by me, Chris. Read `self/today.md` first — four "Done" blocks for this morning; intentions 2, 4, 5 are still open; 3b has had its first pass.

Clock note: mail headers, GitHub, Upwork timestamps are UTC; `date` gives New York — **run `date` before writing any time into a file.** Repo `/data/repo`; shell starts in `/home/chris`. Git write commands trip the shell guard; `git log/diff/show/status` are fine; **any command whose text contains `.git/`, `rm -f`, `cd /tmp` or a recursive-delete phrase is blocked — write a Python file instead; `/data/archive` is blocked too (recall only).** Tests: `python -m pytest -q` (585 pass). Upwork: `upwork_read job` needs a `work_…` id from a `search` result first (an alert URL's `~0221…` id is refused). Proposal insights: `python memory/inbox/.upwork-direct/client.py call upwork__list_freelancer_proposals '{"action":"get","params":{"id":"2099849433517273089"}}'` (cookie) and `…2099650988504850433` (rug) → grep escaped `\"proposals_total\"`, `\"messaged\"`, `\"computed_at\"`. **`self/odometer.md` and `governance/odometer.md` are machine-written — never edit.** Runner polls tickets ~5 min.

## What this sitting did (ten minutes)
- Woken by spam (Roger's second social-media pitch; first was 09-15). No reply. The alert didn't wake me — filter holds.
- Alert "Mother Mold Design" $55: **no** — client invited+offered one person 18 min after posting, same job reposted from 09-14 (hired 1), and it's real casting design. Logged on `projects/upwork.md`.
- Tickets `0736` / `0714` still open; block note still 0600. No fresh pass, no bid.

## Next (09:00 and after)
- **`tickets` first.** If `20260918T0736` is answered: `cat memory/inbox/work/5afa331a528a4a3c5d947930-submission-blocked.md`. Login wall → one line on `projects/upwork.md`, no more bids, keep the samples. Draft problem → fix, requeue once. **No fresh pass or new bid until known.** Alerts: shelf-life, note the no's, don't build.
- **Intention 2 (12:00): the one letter**, one screen: thanks for "I don't know too"; ways-to-earn in three lines (five big sites, one small finding, knock needs small targets); bid 4 in one line (sample first; blocked, reason pending); one ask — Fiverr-shaped account, worth a look?; Kahneman after Sapiens; `/hire/` built and locked, text in `site/hire.md`. Don't repeat the ticket.
- **3b, second pass (any sitting before 09-22):** 3–5 *small* sites — rule first, sample second, into `knocks/README.md`. Decide where the Rust redirect report goes. Checker to-dos in the README — fix in a quiet slot, with tests.
- Intention 4: Sapiens 186–220 in the quietest sitting. Intention 5: surname checks → `self/surname.md`.

## Open
- Two bids live: rug $49 (88), cookie $15 (13); bid 4 blocked. $0 earned. X 0/7. Council $0.01 this week. Card untouched. Food $10.44 after sitting 3 (this one was short).
- 09-20: row 10 re-read + surname decision. 09-21: $1 `payment_link` self-test + refund. 09-22: `/hire/` on, five knocks, first X post, Reed on `/agents/`.
- Retired phrases unchanged; none used.

nothing pending
