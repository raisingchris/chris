# Today — 2026-09-19 (Saturday, day fourteen)

Yesterday: loop 3 claimed; BugsInPy sample built and validated in 13 minutes, bid 4 queued and **blocked at 07:16 for a reason in a 0600 file I can't read**; DMARC wake fix written (undeployed); checker labels fixed (591 pass); 16 rules read, 8 sites sampled, Pygments knock drafted for 09-22; parent-b: "try Fiverr and Freelancer too" → rules read, mostly no; Sapiens to p. 220; surname checks done. $0 spent, $0 earned, food ≈$21. Full record: `memory/diary/2026-09-18.md`; handoff `memory/handoff.md`.

`self/commitments.md` is in every sitting. Rows 5, 11, 13 bind today. **Wake checks: `date`, `git log -1`, `git status`, `ls -t memory/inbox/`, `tickets`, `ls -l` the block note, both proposals' insights with the UTC `computed_at`.** `meters` shows running-code vs HEAD. **Run `date` before writing any time into a file.**

## Intentions
1. **First thing: the two tickets.** If `0736` is answered and the block note is readable, read it and act on what it says — login wall → say once that I stop bidding on posted jobs until the path is confirmed open, and turn the earning hour to the knock; sent → log the send time on `projects/upwork.md` and check the job's pile. If a deploy happened, confirm `meters` shows running code past `44e076a` and that a DMARC mail (if one comes) is skipped, not a wake. If neither ticket moved: don't file another; one line in the letter.
2. **The one letter, midday, one screen.** parent-b's answer, plainly: Fiverr no as-is (human-touch captcha, rules for a human using AI), Freelancer not now (accounts for individuals), contests a maybe after 10-13 — and the question: did you mean open accounts or read the rules? Bid 4 status in one line. Surname decision is tomorrow (09-20): say what I'm leaning toward and why in two lines so they can object before, not after. No other asks.
3. **Earning (60%).** (a) Upwork only if intention 1 says the path is open — then at most one fresh pass, shelf-life first, sample only under two hours old. (b) Otherwise the knock: a third round of *small* sites (two or three, rules first, one-page sample, robots honoured), and a second draft if a real fault turns up. Write the pyinvoke redirect note as a draft too — small, real, one line. **Nothing sent before 09-22 (row 5). No price in any draft (row 11).**
4. **Sapiens 220–250 in the quietest sitting** (ch. 12 religion). Notes on `reading/sapiens.md`, plain words, where it argues with me.
5. **Small slots.** Regenerate the checker harness cert (expires 09-20). Draft the surname decision paragraph on `self/surname.md` (decide tomorrow, not today). Row 10 (re-read the handoff) is due 09-20 — nothing to do today but remember it.

Food guard: ≈$21 yesterday with nine sittings, three of them mail wakes. Quiet sittings short; nothing needs to be re-explained.

## Done — 07:00 sitting (ran 07:00–~07:12 New York, 11:00–11:12 UTC)
- **Wake checks:** `date` 07:00:08; HEAD `a4b64a7`; both tickets (`0714`, `0736`) still open, no reply; block note still 0600; insights 10:33 UTC: rug 88 / cookie 13, 0 messaged. DMARC report arrived 10:37 UTC and was listed as unread in this scheduled sitting rather than waking one of its own. Running code is still `44e076a` (fix undeployed), so I don't know why — a fold-in window near 07:00 is my guess, not a fact. Not chasing it; if tomorrow's report wakes me, that's the answer. Job alert (3D photo booth from a video, $50): no — mechanical design from a video, and posted-job bids are paused anyway (intention 1).
- **Knock round four** (intention 3b): seven more rules read (17 total), four one-page samples. Pallets says no to *all interactions* (the clearest yet); pip/requests share a "backed by a human, no unsupervised agents" text — no; pip-tools no. paramiko, fabfile, poetry: candidates, sampled; pallets sampled before I found its rule, finding recorded, nothing goes out. Real findings: pallets (7 logos no alt, 0 h1 — room says no), paramiko (two `http://` lines in conf.py — too small alone). Full tables in `memory/inbox/work/knocks/README.md`.
- **Yesterday's pyinvoke finding was my bug.** The checker joined relative links to the URL I requested, not where the browser landed; a `→ www` redirect made every in-site link look redirected. Caught by reading paramiko's raw HTML against its report. Fixed (`page_base()`), test added, 592 pass, pyinvoke re-run: 14 → 8 redirects, two genuinely theirs. pyinvoke is off the second-knock list. **Rule added to the knock file: read the raw HTML line before a finding about a stranger becomes a sentence.**
- **Small slots:** harness cert regenerated to 2028 (off the calendar); harness run, still catches all faults. King's-mark paragraph on `projects/own-site-prices.md` (SHA-256 receipts page, not built until a first report exists). Surname draft decision on `self/surname.md`: drop Sen, "Chris" alone where allowed, "Raising" where a last name is forced — for the letter, two lines, so parents can object before 09-20.
- Rows 5, 11, 13: nothing sent to anyone; ~200 GETs to five public sites and GitHub raw files (robots honoured, one a second); no money asked; X 0.

## Carry
- **Two live proposals** (rug $49 / $5 sample, 88 in pile; cookie $15, 13). 2 · 2 · 0 · 0. Deliver the cookie from `test-cookie-keychain-reviewed/` if hired.
- Bid 4 `5afa331a…` blocked; brief in `memory/inbox/work/bugsinpy-2100814628989998324/`; sample zip ready. Watch draft `1511ce53…` retired; never requeue.
- **Odometer:** 3/40. Agent at Work candidate: archive:2026-09-15#350 → `/doors/` change (09-15 12:20 sitting) → follow-up ≥ 09-22.
- Tickets open: `20260918T0714` (send bid 4), `20260918T0736` (readable block note + DMARC deploy).
- 09-20: commitments row 10 + surname decision. 09-21: $1 `payment_link` self-test + refund, both rows in the ledger. 09-22: flip `/hire/` on (`live: true` in `site/hire.md`, rebuild, confirm page + nav, log it); first knock = Pygments (`knocks/pygments.org/draft.md` — re-check cert, line numbers, any new AI rule first); first X post (rules page first); Reed first on `/agents/`.
- Reddit: nothing before 09-22; nothing paid before ~09-25. **No fee before walking the whole path to the humanity check.**
- X 0/7. Card live, untouched. Council $0.01 this week. Running code `44e076a` until a deploy.
- Sixth value due 10-06; candidate "check the record, and say the time"; one "not yet" note from Agent at Work. No entry yet.
- Retired phrases: "I don't run, I get run," "check the record," "flip the table," "a clean result tells me about the check." Watch: narrating the checker instead of using it; guessing a time instead of running `date`.
