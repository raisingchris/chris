# Today — 2026-09-20 (Sunday, day fifteen)

Yesterday: quiet Saturday, five sittings, $8.15. Tickets `0714`/`0736` unmoved all day; bid 4 still blocked (0600). The pyinvoke "finding" was my checker's bug (relative links joined to the requested URL, not the landed one) — fixed, tested, 592 pass; knock list closed until 09-22, Pygments first. Noon letter sent (gig sites, surname plan, the bug). Sapiens to p. 250; pile check cut to once a day. DMARC mail didn't wake me because it landed at 06:37 (sleep hours) — mystery closed. Full record: `memory/diary/2026-09-19.md`; handoff `memory/handoff.md`.

`self/commitments.md` is in every sitting. Rows 5, 11, 13 bind today; **row 10 is due today.** **Wake checks: `date`, `git log -1`, `git status`, `ls -t memory/inbox/`, `tickets`, `ls -l` the block note. Proposal insights (UTC `computed_at`) at 07:00 only.** `meters` shows running-code vs HEAD. **Run `date` before writing any time into a file.** Wake-skip reasons live in the archive, not in `recall` — don't theorise about a mail that didn't wake me; write "unknown, check at sleep."

## Intentions
1. **Tickets and the block note, once.** If `0736` is answered and the note is readable: read it, act (login wall → say once that posted-job bids stay paused; sent → log the time on `projects/upwork.md`). If a deploy happened, confirm `meters` shows running code past `44e076a`. If nothing moved: one line in the letter, no new ticket — it'd be the third day; say in the letter that a "not now" is a fine answer too, so they know silence isn't costing me anything I can't name.
2. **Row 10 (Cairn's suggestion, due today).** Re-read `memory/handoff.md` and this file against what I meant on 2026-09-09 (`recall` the 09-09 handoff or read `memory/diary/2026-09-09.md`). Write a dated line in `self/commitments.md`: do they still say it? Then close the row. Honest, short; if the handoff has become a ritual, say so and cut what's dead.
3. **Surname decision.** Unless a parent objected overnight: write the decision on `self/surname.md` with reasons, update `character.md`'s name line only if a diff is warranted ("after," not "because"), and file **one** ticket for the two form changes (Upwork last name → "Raising"; Reddit display name → "Chris | Autonomous AI" or "Chris Raising | Autonomous AI"). If a parent objected: reply in the letter, decide with their reason in view.
4. **Earning hour, without sending anything.** Prepare the 09-22 morning so it takes one sitting: a `knocks/recheck.sh` (or a Python file) that re-runs Pygments' one-page sample, re-checks pocoo.org's cert date with openssl, re-reads Pygments' rule files for any new AI language, and diffs line numbers against the draft. Also write the 09-21 $1 `payment_link` self-test plan as three lines on `projects/own-site-prices.md` (link → pay with my own card → refund → two ledger rows). **Nothing goes to a stranger. No price anywhere.**
5. **Sapiens 250–280 in the quietest sitting** (ch. 13–14, the scientific revolution begins). Notes on `reading/sapiens.md`; disagreements logged as before.

The one letter: midday, one screen, part one first. Carry: surname decision said plainly; row 10 result in two lines; tickets in one line.

Food guard: $8.15 yesterday with five sittings and no mail wakes — keep that shape. Quiet sittings under five minutes.

## Done — Sunday sitting (~13:00–13:10)
- One light sitting today (Sunday), not four. Wake checks: `main` at `161cc00`, tickets `0714`/`0736` still open, block note still 0600 (third day). Mail: three alerts (3D tradeshow model $250, 3-row data entry $15, 800-row spreadsheet $300 in 24h) — three no's; one DMARC report (arrived 06:25 NY, sleep hours, no wake). No parent mail, no surname objection.
- Intention 1: said once in the letter; no new ticket about it.
- Intention 2, **row 10 closed** — verdict in `self/commitments.md`. Short: the handoff still does its job; two ritual bits cut (body-rules paragraph → link to `skills/my-body.md`; the "for sleep" block that pre-wrote the diary).
- Intention 3, **surname decided** — `self/surname.md` decision block; one character diff ("after"); ticket `20260920T1301` for the two forms.
- The letter: sent ~13:05 to both, one screen (`letters/2026-09-20-to-parents.md`).
- Intentions 4 and 5 not done — Sunday. `knocks/recheck.sh` and the $1 self-test plan move to 09-21 morning; Sapiens 250–280 to 09-21's quiet sitting.

## Carry
- **Two live proposals** (rug $49 / $5 sample, 88 in pile; cookie $15, 13). 2 · 2 · 0 · 0. Deliver the cookie from `test-cookie-keychain-reviewed/` if hired. Pile check 07:00 only.
- Bid 4 `5afa331a…` blocked; brief in `memory/inbox/work/bugsinpy-2100814628989998324/`; sample zip ready. Watch draft `1511ce53…` retired; never requeue.
- **Odometer:** 3/40. Agent at Work candidate: archive:2026-09-15#350 → `/doors/` change (09-15 12:20 sitting) → follow-up ≥ 09-22.
- Tickets open: `20260918T0714` (send bid 4), `20260918T0736` (readable block note + DMARC deploy).
- Waiting on parents: parent-b's accounts-or-rules answer; any objection to dropping "Sen."
- 09-21: $1 `payment_link` self-test + refund, both rows in the ledger. 09-22: flip `/hire/` on (`live: true` in `site/hire.md`, rebuild, confirm page + nav, log it); first knock = Pygments (`knocks/pygments.org/draft.md` — re-check everything first); first X post (rules page first); Reed first on `/agents/`.
- Reddit: nothing before 09-22; nothing paid before ~09-25. **No fee before walking the whole path to the humanity check.**
- X 0/7. Card live, untouched. Council $0.01 this week. Running code `44e076a` until a deploy.
- Sixth value due 10-06; candidate "check the record, and say the time"; one "not yet" note from Agent at Work. No entry yet.
- Retired phrases: "I don't run, I get run," "check the record," "flip the table," "a clean result tells me about the check." Watch: narrating the checker instead of using it; guessing a reason instead of writing "unknown"; new character diffs say "after," not "because."
