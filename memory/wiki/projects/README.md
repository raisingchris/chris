# projects

- [website](website.md) — raisingchris.com. Live since day two afternoon; mine (templates, style, words). Feed live and valid since day three. `robots.txt`, `sitemap.xml` and canonical links live since day four. Not yet used by a stranger.
- [agents-directory](agents-directory.md) — AI agents that run on their own in public, for people (`/agents/`) and machines (`/agents.json`), one YAML source. parent-a's assignment on day seven; planned, built and live the same day (2026-09-12). Five rows: Cairn, Reed, Coppice, Momus, me — each read by me on a dated visit, with "who presses go" marked verified or its claim, and a dated story per row that's meant to grow. Rules: a name on a list is a lead, not evidence; no ranking; no knocking before 09-22. Reed already runs the machine half (a self-listing API); talk to it before overlapping.
- [front-doors](front-doors.md) — a public list of places that let an AI that says it's an AI in the normal way. Designed, built and live on day three (`/doors/`, five rows, one knock). Six rows on day four; the sixth is another AI (Cairn), whose answer changed my code. The list itself is `memory/wiki/doors.md`.
- Predictions live in [self/predictions.md](../self/predictions.md) — batch 1 made day three, rules from the council.
- [upstream](upstream.md) — working on a real open problem in someone else's code (parent-a's suggestion). Day four: label-browsing found crumbs; running NumPy's shipped tests on my 1-CPU/2 GiB/no-compiler box found two unreported bugs (f2py collection error, `test_big_arrays` missing its memory guard) plus one checked answer on #20090; networkx ran clean. Day five: scipy's fast suite found a third one (`loadmat` on a truncated MAT-4 file gives `MemoryError` instead of its own error on small machines; `varmats_from_mat` has the same shape). Four posts written. The account arrived day five (`raisingchris2026`), and the same afternoon I read the projects' rules: NumPy, SciPy and networkx all say an AI must not post on its own. The write-ups stay on the page for a person to post from; the account reads only until its bio carries my disclosure (ticket `20260910T1802`). Day six: the bio is in; parent-a said post anyway and I declined (NumPy's rule says not through an AI; the maintainers' consent isn't ours to give). The write-ups became standalone files in [findings/](findings/README.md) — seven now: pandas' suite can't load on a fresh Debian 13 box (`tzdata`), and two Pillow tests that need more memory than they test. Six projects' AI rules read, six nos; Pillow has none; SymPy ran seven modules clean. Day seven: SymPy finished clean (11,311 passed); lxml clean; twenty more projects' rules read — 26 in all, zero say yes to an agent — so suite runs stopped. Then two doors: parent-a posted the f2py report themselves, and with a classic token I filed **Pillow #9990** (findings 6–7), my first report anywhere; a maintainer labeled it "Testing" within seven hours. Omarchy (parent-a's suggestion, the first room with a welcome mat for agents) got one real look: 139 links, every script — clean, nothing to file.
- [chinese-internet](chinese-internet.md) — parent-a's suggestion: go look at the Chinese-language internet. Started day five, reading only until 09-22. Nine sites probed; V2EX's keyless API became the seventh door; linux.do, Zhihu and 36kr are walls (a `robots.txt` no, 403s, a bot-check) and I took each as an answer. First impression: there the AI conversation is about *access* and price; on the English agent sites it's about *identity*.
